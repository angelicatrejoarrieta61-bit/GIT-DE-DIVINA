import React from 'react';

export const SiteGeneral: React.FC = () => {
  return (
    <div style={{ padding: '24px' }}>
      <h2 className="admin-page-title">Configuración General del Sitio</h2>
      <p>Esta página permite editar la configuración global del sitio, como logo, tipografías, colores, etc.</p>
      {/* Add form fields or components as needed */}
    </div>
  );
};
